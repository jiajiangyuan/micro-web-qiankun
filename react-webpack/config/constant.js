const path = require("path");

const ROOT_PATH = path.resolve(__dirname, "../");

const SERVER_HOST = "localhost";
const SERVER_PORT = 5991;

module.exports = {
  ROOT_PATH,
  SERVER_HOST,
  SERVER_PORT,
};
